<!DOCTYPE html>
<html lang="en">
    <%- include('../partials/head',{ title:'home', }) %>

    <body id="b0dy__c0nt41n3r">
        <div class="body--form">
            <%- include('../partials/logoForms',{ title:'Perfil de usuario' }) %>
            
            <div class="body__profile--box">
                <div class="body__profile--title">
                    <h3>👋 ¡Hola, bienvenido <span><%= user.userName %></span>!</h3>
                </div>

                <div class="body__profile--userData">
                    <div class="body__profile--avatarAndLocation">
                        <div class="body__profile--avatar">
                            <img src="/images/users/<%= user.avatar ? user.avatar.file : 'DEFAULT-IMAGE.jpg' %>" name ="avatar" id="avatar" alt="avatar">
                        </div>
    
                        <div>
                            <p><%= user.address.street %>, <%= user.address.city %>, <%= user.address.province %></p>
                        </div>
                    </div>

                    <div class="body__profile--userInfo">
                        <div class="body__profile__userInfo--fullName">
                            <p><%= user.firstName %> <%= user.lastName %></p>
                        </div>
                        
                        <div class="body__profile__userInfo--birthday">
                            <p><%= moment(user.birthday).format('DD-MM-YYYY') %></p>
                        </div>
                    </div>

                    <div class="body__profile--contact">
                        <div class="body__profile__contact--phone">
                            <p><%= user.phone %></p>
                        </div>
    
                        <div class="body__profile__contact--email">
                            <p><%= user.email %></p>
                        </div>
                    </div>

                    <div class="body__profile--aboutMe">
                        <div class="body__profile__aboutMe--text">
                            <h4>Acerca de mí...</h4>
                            <p><%= user.aboutMe %></p>
                        </div>
                    </div>

                    <div class="body__profile--products">
                        <div class="body__profile__product--productsInSale">
                            <h4>Mis productos en venta</h4>
                            <p>acá irán los productos a la venta</p> <!-- acá irán los productos del usuario -->
                        </div>
                    </div>

                </div>

                <div class="body__profile--edit">
                    <a href="/users/profileUpdate">Editar Perfil</a>
                </div>

            </div>
        </div>
    </body>

</html>